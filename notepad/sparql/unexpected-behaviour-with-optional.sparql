PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

select ?subject ?label ?children ?p
WHERE {
    { ?subject rdfs:label ?label FILTER (regex(?label, "test container 2", "i")) }

    OPTIONAL {
        ?subject ?p ?children FILTER sameTerm(?p, rdfs:member)

#   Doesn't work as I expect
#
#    ?subject rdfs:member ?children

    }
}